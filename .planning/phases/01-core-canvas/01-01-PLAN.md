---
phase: 01-core-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .npmrc
  - tsconfig.base.json
  - .gitignore
  - packages/excalidraw/package.json
  - packages/excalidraw/FORK_CHANGES.md
  - packages/shared/package.json
  - packages/shared/src/types/canvas.ts
  - packages/shared/src/types/project.ts
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/App.css
  - apps/web/src/vite-env.d.ts
  - apps/web/tsconfig.json
  - apps/web/tsconfig.node.json
autonomous: true
requirements:
  - CANVAS-01
user_setup: []
must_haves:
  truths:
    - "Root workspace resolves pnpm install without errors"
    - "Vendored Excalidraw source exists at packages/excalidraw/src/ with a complete src/ tree"
    - "FORK_CHANGES.md documents the initial vendored state"
    - "packages/shared/ exports AIElement and Project types"
    - "apps/web/ dev server starts with Vite 8 and renders a 'Hello Canvas' page"
    - "pnpm workspaces allow cross-package imports (@ac-canvas/excalidraw, @ac-canvas/shared)"
  artifacts:
    - path: "package.json"
      contains: '"private": true'
    - path: "pnpm-workspace.yaml"
      contains: "packages:"
    - path: "packages/excalidraw/FORK_CHANGES.md"
      contains: "Initial vendored fork"
    - path: "apps/web/vite.config.ts"
      contains: "defineConfig"
  key_links:
    - from: "apps/web/package.json"
      to: "packages/excalidraw/package.json"
      via: "pnpm workspace @ac-canvas/excalidraw dependency"
      pattern: "@ac-canvas/excalidraw"
    - from: "apps/web/package.json"
      to: "packages/shared/package.json"
      via: "pnpm workspace @ac-canvas/shared dependency"
      pattern: "@ac-canvas/shared"
---

<objective>
Set up the complete monorepo foundation for the project: pnpm workspace configuration, vendored Excalidraw v0.18.x fork with FORK_CHANGES.md, shared types package, and a Vite 8 + React 19 + TailwindCSS v4 web app skeleton.

Purpose: This is the infrastructure every subsequent plan depends on. The monorepo structure, package interdependencies, and build toolchain must be correct before any feature code can be written.
Output: A working monorepo with pnpm workspace resolving, Excalidraw vendored at packages/excalidraw/, shared types at packages/shared/, and a Vite 8 dev server that starts successfully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-canvas/01-CONTEXT.md
@.planning/phases/01-core-canvas/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root monorepo configuration</name>
  <read_first>
    - .planning/phases/01-core-canvas/01-CONTEXT.md (D-03 monorepo structure, D-04 pnpm workspace)
    - .planning/phases/01-core-canvas/01-RESEARCH.md (pnpm-workspace.yaml example, .npmrc config)
  </read_first>
  <files>
    - package.json (root)
    - pnpm-workspace.yaml
    - .npmrc
    - tsconfig.base.json
    - .gitignore
  </files>
  <action>
    Create the workspace root at J:/ai-creative-canvas/.

    1. Upgrade pnpm to v11: `npm install -g pnpm@11` (per RESEARCH.md: pnpm 10.10.0 currently installed, v11 is required for workspace features).

    2. Create `package.json` (root) with:
    ```json
    {
      "name": "ai-creative-canvas",
      "private": true,
      "packageManager": "pnpm@11.8.0",
      "scripts": {
        "dev": "pnpm --filter @ac-canvas/web dev",
        "build": "pnpm --filter @ac-canvas/web build",
        "lint": "pnpm -r lint",
        "typecheck": "pnpm -r typecheck"
      },
      "engines": {
        "node": ">=22.12.0"
      }
    }
    ```

    3. Create `pnpm-workspace.yaml`:
    ```yaml
    packages:
      - 'apps/*'
      - 'packages/*'
    ```

    4. Create `.npmrc` (pnpm v11 supply-chain settings, relaxed for local dev):
    ```
    pnpm-minimum-release-age=0
    pnpm-allow-build=esbuild,rollup,*sharp
    shamefully-hoist=true
    ```

    5. Create `tsconfig.base.json`:
    - `compilerOptions`: `target: "ES2022"`, `moduleResolution: "bundler"`, `module: "ESNext"`, `strict: true`, `jsx: "react-jsx"`, `declaration: true`, `declarationMap: true`, `sourceMap: true`, `skipLibCheck: true`
    - Include `"$schema": "https://json.schemastore.org/tsconfig"`

    6. Create `.gitignore`:
    ```
    node_modules/
    dist/
    .turbo/
    *.local
    ```
  </action>
  <verify>
    <automated>pnpm --version 2>/dev/null | grep -q "^11" && echo "pnpm 11 OK" || echo "pnpm upgrade needed"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at workspace root with `"private": true` and `"packageManager": "pnpm@11.8.0"`
    - `pnpm-workspace.yaml` exists with `packages: ['apps/*', 'packages/*']`
    - `.npmrc` exists with `shamefully-hoist=true`
    - `tsconfig.base.json` exists with `"jsx": "react-jsx"` and `"strict": true`
    - `.gitignore` exists with `node_modules/` and `dist/`
    - `pnpm --version` outputs 11.x
  </acceptance_criteria>
  <done>Root workspace config files created and pnpm upgraded to v11</done>
</task>

<task type="auto">
  <name>Task 2: Vendored Excalidraw fork at packages/excalidraw/ with FORK_CHANGES.md</name>
  <read_first>
    - .planning/phases/01-core-canvas/01-CONTEXT.md (D-01 full vendored fork, D-02 fork base v0.18.x, D-25 sync cadence, D-26 FORK_CHANGES.md discipline)
    - .planning/phases/01-core-canvas/01-RESEARCH.md (Element type system, dual-canvas architecture, fork strategy)
  </read_first>
  <files>
    - packages/excalidraw/package.json
    - packages/excalidraw/FORK_CHANGES.md
    - packages/excalidraw/src/ (full vendored Excalidraw source tree)
    - packages/excalidraw/tsconfig.json
  </files>
  <action>
    Per D-01 and D-02: Fork Excalidraw from the v0.18.x release tag that supports React 19. Clone the upstream repository and vendor the entire source.

    1. Create packages/excalidraw/ directory structure.

    2. Clone Excalidraw upstream at v0.18.x release tag:
    ```bash
    git clone --depth 1 --branch v0.18.0 https://github.com/excalidraw/excalidraw.git /tmp/excalidraw
    ```
    If v0.18.0 doesn't exist as a tag, use the earliest v0.18.x tag (v0.18.1, etc.). Verify it supports React 19 by checking that PR #9182 is included (search the commit log for "React 19" in the cloned repo).
    If the tag is missing altogether, use the latest v0.17.x tag and note the React 19 risk in FORK_CHANGES.md.

    3. Copy the entire source tree from the clone to our packages/excalidraw/src/:
    ```bash
    cp -r /tmp/excalidraw/src/* packages/excalidraw/src/
    ```
    Also copy any assets, locales, or CSS that Excalidraw needs at runtime (check the clone's packages/excalidraw/src/ directory structure).

    4. Read the cloned Excalidraw's package.json (at /tmp/excalidraw/package.json or /tmp/excalidraw/packages/excalidraw/package.json) to extract the actual dependencies. Create packages/excalidraw/package.json:

    ```json
    {
      "name": "@ac-canvas/excalidraw",
      "version": "0.18.0",
      "private": true,
      "type": "module",
      "main": "./src/index.ts",
      "exports": {
        ".": "./src/index.ts",
        "./element/types": "./src/element/types.ts",
        "./renderer/renderElement": "./src/renderer/renderElement.ts",
        "./scene/Renderer": "./src/scene/Renderer.ts",
        "./element/newElement": "./src/element/newElement.ts"
      },
      "dependencies": {
        "roughjs": "^4.6.6",
        "react": "^19.0.0",
        "react-dom": "^19.0.0"
      }
    }
    ```
    IMPORTANT: Use the actual dependencies from the cloned Excalidraw package.json. Do NOT guess them. Copy them verbatim.

    5. Clean up:
    ```bash
    rm -rf /tmp/excalidraw
    ```

    6. Create `packages/excalidraw/FORK_CHANGES.md` with the following template:
    ```markdown
    # FORK_CHANGES.md — @ac-canvas/excalidraw

    **Upstream:** [excalidraw/excalidraw](https://github.com/excalidraw/excalidraw)
    **Fork base tag:** v0.18.0
    **Fork date:** 2026-06-29

    ## Purpose
    Vendored fork of Excalidraw v0.18.x to add AIElement type support and deep integration
    with the project's custom rendering pipeline (chunk rendering, resolution-tiered rendering,
    LRU image cache). Full vendored copy (not npm wrapper) per D-01.

    ## Sync Cadence
    - Sync upstream every 2-4 weeks (D-25)
    - Before each sync, review this file to identify customizations that may conflict
    - Any generic (non-domain-specific) bug fix should be PR'd upstream (D-27)

    ## Modified Files

    | File | Modification | Reason | Date | Phase |
    |------|-------------|--------|------|-------|
    | (initial vendored state) | Vendored from upstream v0.18.0 | Initial fork | 2026-06-29 | Phase 1 |

    ## Modification Rules
    - Every phase that touches packages/excalidraw/ MUST update this file
    - Entries: file path, modification reason, date, phase reference
    - No exception to this discipline (D-26)
    ```

    7. Create `packages/excalidraw/tsconfig.json`:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src"
      },
      "include": ["src"]
    }
    ```
  </action>
  <verify>
    <automated>test -d packages/excalidraw/src && test -f packages/excalidraw/FORK_CHANGES.md && echo "Fork OK" || echo "Fork MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/excalidraw/src/` exists with a complete Excalidraw source tree (contains `index.ts` or `index.tsx`)
    - `packages/excalidraw/package.json` exists with `"name": "@ac-canvas/excalidraw"`
    - `packages/excalidraw/FORK_CHANGES.md` exists with "Fork base tag: v0.18" and "Modified Files" table
    - `packages/excalidraw/tsconfig.json` exists extending tsconfig.base.json
    - The clone was removed (no /tmp/excalidraw)
  </acceptance_criteria>
  <done>Excalidraw vendored fork at packages/excalidraw/ with FORK_CHANGES.md tracking</done>
</task>

<task type="auto">
  <name>Task 3: Create packages/shared/ and apps/web/ skeleton</name>
  <read_first>
    - .planning/phases/01-core-canvas/01-CONTEXT.md (D-03 monorepo structure: packages/shared/, apps/web/)
    - .planning/phases/01-core-canvas/01-RESEARCH.md (AIElement type pattern, Project type, Vite 8 config, Tailwind v4 CSS-first config)
    - .planning/ROADMAP.md (Phase 1 success criteria)
  </read_first>
  <files>
    - packages/shared/package.json
    - packages/shared/tsconfig.json
    - packages/shared/src/index.ts
    - packages/shared/src/types/canvas.ts
    - packages/shared/src/types/project.ts
    - packages/shared/src/utils/coordinate.ts
    - packages/shared/src/utils/serialization.ts
    - apps/web/package.json
    - apps/web/vite.config.ts
    - apps/web/tsconfig.json
    - apps/web/tsconfig.node.json
    - apps/web/index.html
    - apps/web/src/main.tsx
    - apps/web/src/App.tsx
    - apps/web/src/App.css
    - apps/web/src/vite-env.d.ts
  </files>
  <action>
    Create two workspace packages: packages/shared/ (types and utilities) and apps/web/ (Vite 8 SPA entry).

    === packages/shared/ ===

    1. Create `packages/shared/package.json`:
    ```json
    {
      "name": "@ac-canvas/shared",
      "version": "0.1.0",
      "private": true,
      "type": "module",
      "main": "./src/index.ts",
      "exports": {
        ".": "./src/index.ts",
        "./types/canvas": "./src/types/canvas.ts",
        "./types/project": "./src/types/project.ts",
        "./utils/coordinate": "./src/utils/coordinate.ts",
        "./utils/serialization": "./src/utils/serialization.ts"
      },
      "devDependencies": {
        "typescript": "^5.0.0"
      }
    }
    ```

    2. Create `packages/shared/tsconfig.json` extending base:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src"
      },
      "include": ["src"]
    }
    ```

    3. Create `packages/shared/src/index.ts`:
    ```typescript
    export * from './types/canvas'
    export * from './types/project'
    export * from './utils/coordinate'
    export * from './utils/serialization'
    ```

    4. Create `packages/shared/src/types/canvas.ts`:
    - Export `GenerationStatus` type: `'idle' | 'queued' | 'generating' | 'done' | 'error'`
    - Export `AIElement` interface extending `Omit<ExcalidrawElement, 'type'>` with `type: 'ai-image'`, plus `prompt: string`, `aiProvider: string`, `generationParams: Record<string, unknown>`, `generationStatus: GenerationStatus`, `imageBlobId: string | null`
    - Import `ExcalidrawElement` from `@ac-canvas/excalidraw/element/types`
    - Export `CanvasSerializedState` with `elements: Array<any>` and `viewport: { x: number; y: number; zoom: number }`
    - Export `Viewport` interface with `x: number; y: number; zoom: number`

    5. Create `packages/shared/src/types/project.ts`:
    - Export `Project` interface: `id?: number; name: string; canvasState: string; viewport: string; createdAt: Date; updatedAt: Date`

    6. Create `packages/shared/src/utils/coordinate.ts`:
    - Export stub functions: `canvasToNodeSpace(x, y, viewport)`, `nodeToCanvasSpace(x, y, viewport)` — both return `{ x, y }`. The body should throw `new Error('Not implemented: Phase 2')`. Per RESEARCH.md, these are stubs until React Flow integration in Phase 2.

    7. Create `packages/shared/src/utils/serialization.ts`:
    - Export `deepClone<T>(value: T): T` using `structuredClone(value)` with a try/catch fallback to `JSON.parse(JSON.stringify(value))` (per RESEARCH.md A3 assumption about structuredClone availability)
    - Export `filterTransientProps(element: any): any` — removes `isSelected`, `dragging`, `measured` properties from an element object (per D-09 and Pitfall 5 mitigation)

    === apps/web/ ===

    8. Create `apps/web/package.json`:
    ```json
    {
      "name": "@ac-canvas/web",
      "version": "0.1.0",
      "private": true,
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "typecheck": "tsc --noEmit"
      },
      "dependencies": {
        "react": "^19.0.0",
        "react-dom": "^19.0.0",
        "zustand": "^5.0.0",
        "immer": "^11.0.0",
        "dexie": "^4.0.0",
        "zod": "^4.0.0",
        "@ac-canvas/excalidraw": "workspace:*",
        "@ac-canvas/shared": "workspace:*"
      },
      "devDependencies": {
        "typescript": "^5.0.0",
        "@types/react": "^19.0.0",
        "@types/react-dom": "^19.0.0",
        "vite": "^8.0.0",
        "@vitejs/plugin-react": "^4.0.0",
        "tailwindcss": "^4.0.0",
        "@tailwindcss/vite": "^4.0.0",
        "vitest": "^4.0.0"
      }
    }
    ```

    9. Create `apps/web/vite.config.ts`:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    // Vite 8 with Rolldown bundler
    // See: https://vite.dev/blog/announcing-vite8
    export default defineConfig({
      plugins: [
        react(),
        tailwindcss(), // TailwindCSS v4 Vite plugin
      ],
      server: {
        port: 5173,
        open: true,
      },
      // Vitest configuration (inline, inherits Vite config)
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: ['./src/test/setup.ts'],
      },
    })
    ```

    10. Create `apps/web/tsconfig.json`:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",
        "types": ["vitest/globals"]
      },
      "include": ["src"],
      "references": [{ "path": "./tsconfig.node.json" }]
    }
    ```

    11. Create `apps/web/tsconfig.node.json`:
    ```json
    {
      "extends": "../../tsconfig.base.json",
      "compilerOptions": {
        "composite": true,
        "module": "ESNext",
        "moduleResolution": "bundler",
        "allowSyntheticDefaultImports": true
      },
      "include": ["vite.config.ts"]
    }
    ```

    12. Create `apps/web/index.html`:
    ```html
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>AI Creative Canvas</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    13. Create `apps/web/src/vite-env.d.ts`:
    ```typescript
    /// <reference types="vite/client" />
    ```

    14. Create `apps/web/src/main.tsx`:
    ```typescript
    import React from 'react'
    import ReactDOM from 'react-dom/client'
    import App from './App'
    import './App.css'

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <App />
      </React.StrictMode>
    )
    ```

    15. Create `apps/web/src/App.tsx` (minimal shell - per D-06, keep Excalidraw UI intact, no custom toolbar/sidebar):
    ```typescript
    function App() {
      return (
        <div className="w-screen h-screen overflow-hidden">
          {/* CanvasWrapper will be added in Plan 03 */}
          <div className="flex items-center justify-center h-full text-gray-400 select-none">
            <p>Canvas placeholder — CanvasWrapper coming in Plan 03</p>
          </div>
        </div>
      )
    }

    export default App
    ```

    16. Create `apps/web/src/App.css` (TailwindCSS v4 CSS-first config per RESEARCH.md pattern):
    ```css
    @import "tailwindcss";

    @theme {
      --color-primary: #6c5ce7;
      --color-primary-light: #a29bfe;
      --color-surface: #ffffff;
      --color-surface-secondary: #f5f5f5;
      --font-sans: 'Inter', 'system-ui', sans-serif;
    }

    /* Reset full viewport */
    html, body, #root {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }
    ```

    17. Create `apps/web/src/test/setup.ts`:
    ```typescript
    import '@testing-library/jest-dom/vitest'
    ```

    18. Run install:
    ```bash
    pnpm install
    ```

    19. Verify the dev server starts (run in background, check for success message):
    ```bash
    cd apps/web && npx vite --host 0.0.0.0 &
    sleep 5 && curl -s http://localhost:5173 | head -20
    kill %1 2>/dev/null
    ```
  </action>
  <verify>
    <automated>pnpm --filter @ac-canvas/web ls --depth 0 2>/dev/null | grep -q "@ac-canvas/excalidraw" && echo "Workspace deps OK" || echo "Deps MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/package.json` exists with `"name": "@ac-canvas/shared"`
    - `packages/shared/src/types/canvas.ts` exports `AIElement` and `CanvasSerializedState`
    - `packages/shared/src/types/project.ts` exports `Project` interface with all required fields
    - `packages/shared/src/utils/serialization.ts` exports `deepClone` and `filterTransientProps`
    - `apps/web/package.json` has workspace dependencies on `@ac-canvas/excalidraw` and `@ac-canvas/shared`
    - `apps/web/vite.config.ts` uses `@vitejs/plugin-react` and `@tailwindcss/vite` plugins
    - `apps/web/src/App.css` starts with `@import "tailwindcss"`
    - `apps/web/src/App.tsx` renders a placeholder div
    - `pnpm install` completes without errors
    - `apps/web/src/test/setup.ts` exists with vitest jest-dom import
  </acceptance_criteria>
  <done>Shared package with types and utilities, web app skeleton with Vite 8 + React 19 + Tailwind v4, all dependencies installed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client | Phase 1 is entirely client-side with no server communication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S (Spoofing) | N/A | Accept | Phase 1 has no authentication. No server, no sessions. Spoofing not applicable. |
| T-01-02 | T (Tampering) | N/A | Accept | All state is client-side (Zustand stores, IndexedDB). No data in transit. Tampering requires browser access, which already has full access. |
| T-01-03 | I (Information Disclosure) | N/A | Accept | No network communication. No sensitive data stored (AI API keys, user data). |
| T-01-04 | D (Denial of Service) | Excalidraw canvas | Mitigate | Chunk rendering (Plan 05) prevents UI thread blocking with 500+ elements. 200MB LRU cache limit (Plan 05) prevents memory exhaustion from decoded images. |
</threat_model>

<verification>
- [ ] `pnpm install` succeeds in workspace root
- [ ] `pnpm ls -r` shows all 3 workspace packages
- [ ] `npx tsc --noEmit -p apps/web/tsconfig.json` type-checks without errors (known: Excalidraw type errors may exist from strict mode — these are acceptable in the vendored fork and will be resolved as we modify files in Plan 02+)
- [ ] `npx tsc --noEmit -p packages/shared/tsconfig.json` type-checks without errors
</verification>

<success_criteria>
- Workspace resolves 3 packages: @ac-canvas/excalidraw, @ac-canvas/shared, @ac-canvas/web
- Excalidraw source vendored at packages/excalidraw/src/ with 100+ source files
- FORK_CHANGES.md created with initial entry
- packages/shared/ exports AIElement type, Project type, coordinate stubs, serialization utilities
- apps/web/ starts a Vite 8 dev server that serves index.html
- All dependencies installed and lockfile generated
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-canvas/01-01-SUMMARY.md` with:
- Workspace configuration details
- Excalidraw fork base tag and any issues encountered
- Shared package exports
- Dev server verification result
- Key decisions implemented (D-01 through D-06, D-25, D-26)
</output>
